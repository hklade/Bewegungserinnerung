org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# AGP 9 built-in Kotlin opt-out: this project uses the classic
# org.jetbrains.kotlin.android plugin (compatible with the current
# Compose compiler / KSP / Room Gradle plugin ecosystem).
android.builtInKotlin=false
android.newDsl=false
android.sync.suppressAgpWarnings=DEPRECATED_DSL
